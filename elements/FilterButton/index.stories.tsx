import { FilterButton } from "./index";

export default {
  title: "Elements/FilterButton",
  component: FilterButton,
};

export const Primary = {
  args: {
    label: "Filter",
    open: false,
    onClick: () => {},
  },
};
